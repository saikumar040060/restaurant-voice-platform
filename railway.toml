[build]
builder = "RAILPACK"
buildCommand = "mvn -B -f apps/backend/pom.xml -DskipTests package"

[deploy]
startCommand = "java -jar apps/backend/target/restaurant-voice-platform-0.0.1-SNAPSHOT.jar"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
