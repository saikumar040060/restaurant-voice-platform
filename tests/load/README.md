# Load tests

Initial pilot target: five concurrent calls per location; also demonstrate ten isolated concurrent calls. Declare hardware, provider mocks versus real services, duration, latency, error rates, and queue limits. No load results exist yet.
