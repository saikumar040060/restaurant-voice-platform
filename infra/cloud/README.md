# Cloud deployment

Not configured. Hosting provider, budget, approved accounts, and reproducible remote execution must be agreed before provisioning. No public deployment is authorized. Local execution must never be described as continuing after laptop sleep.
